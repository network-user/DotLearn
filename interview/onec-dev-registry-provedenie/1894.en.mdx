---
id: 1894
title: "Locks during document posting in 1C"
category: onec-dev-registry-provedenie
categoryLabel: "Регистры, проведение"
stage: tech
stageLabel: "Tech"
lang: en
---

# Locks during document posting in 1C

## In short

Managed data locks (`БлокировкаДанных`) are set before reading and changing balances in registers. Exclusive locks on dimensions prevent races on parallel warehouse issues.

## How to answer in an interview

- I lock register dimensions I change before the balance query.
- I use `РежимБлокировкиДанных.Исключительный` for issues.
- The lock lasts until the posting transaction ends.
- Platform auto locks do not remove the need for explicit locks on hot registers.
- I am honest: too broad a lock (whole warehouse) serializes all users.

## How it works

```bsl
Блокировка = Новый БлокировкаДанных;
Элемент = Блокировка.Добавить("РегистрНакопления.ТоварыНаСкладах");
Элемент.Режим = РежимБлокировкиДанных.Исключительный;
Элемент.УстановитьЗначение("Склад", Склад);
Элемент.УстановитьЗначение("Номенклатура", Номенклатура);
Блокировка.Заблокировать();
```

After the lock I read balance and build the issue.

## Pitfalls

**No lock.** Negative stock under parallel sales.
**Locking whole register.** Deadlocks and queues.
**Lock after read.** Classic read-then-write race.
**Long transaction.** Holding the lock blocks others.
**Direct SQL bypass.** Platform locks do not apply.
