---
id: 2083
title: "Garbage collector types in HotSpot"
category: java-jvm-i-pamyat
categoryLabel: "JVM и память"
stage: tech
stageLabel: "Tech"
lang: en
---

# Garbage collector types in HotSpot

## In short

HotSpot offers several interchangeable GC implementations selected by JVM flags. All use generational or region heap layout and mark reachability from roots, but differ in parallelization and concurrent phases. JDK 21 default is G1; ZGC and Shenandoah for ultra-low pause; Parallel for batch throughput.

## How to answer in an interview

- Young gen collectors: Serial, Parallel (stop-the-world copy/evacuate).
- Old gen: Parallel Old, G1 mixed, ZGC/Shenandoah concurrent relocate.
- CMS is legacy, removed; do not confuse with G1 concurrent mark.
- Generational hypothesis drives frequent young / rare old strategy.
- Honestly: no single "best" GC; P99 latency + allocation rate metrics decide.

## How it works

Generational layout (classic):

```
┌──────── Young ────────┬──────── Old ────────┐
│ Eden │ S0 │ S1        │ Tenured regions    │
└───────────────────────┴─────────────────────┘
```

Minor GC (Parallel example):

1. STW: mark live in Eden+From survivor.
2. Copy survivors to To survivor or promote Old.
3. Clear Eden.

G1 divides heap into equal regions (~1-32MB); tracks garbage per region, collects highest garbage-first in mixed cycles.

Enable examples:

```bash
# Throughput
java -XX:+UseParallelGC -jar etl.jar

# Balanced (default many JDKs)
java -XX:+UseG1GC -jar api.jar

# Low latency
java -XX:+UseZGC -jar realtime.jar
```

JVM prints active collector at startup with `-Xlog:gc*` (Java 9+ unified logging).

## Pitfalls

- **Conflicting flags.** Multiple `-XX:+Use*GC` → last wins or JVM error.
- **Large heap + Parallel.** Full GC pause scales with live set size.
- **Ignoring GC cause logs.** Allocation failure vs metadata GC need different fixes.
- **Assuming young-only tuning fixes Old gen pressure.** Promotion rate matters.
- **Different JDK vendors.** OpenJDK vs Amazon Corretto same flags mostly; verify release notes.
