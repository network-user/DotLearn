---
id: 3062
title: "What is Jira and how does QA work with tickets?"
category: qa-instrumenty-postman-swagger-charles-i-dr
categoryLabel: "Инструменты: Postman, Swagger, Charles и др"
stage: tech
stageLabel: "Tech"
lang: en
---

# What is Jira and how does QA work with tickets?

## In short

Jira (Atlassian) is a TMS/issue tracker: Bug, Story, Task, Epic, workflow statuses, sprint board, blocks/relates links, Environment, Priority, Components fields. QA files bugs with repro steps, actual/expected, attachments, links to story and release version; filters open defects for regression and release sign-off.

## How to answer in an interview

- Bug template: title, steps, expected, actual, env, build, severity/priority.
- Workflow: Open → In Progress → Ready for QA → Done / Reopen.
- JQL: `project = SHOP AND type = Bug AND status != Done AND fixVersion = 4.3.0`.
- Link issue: blocks, duplicates, relates to story SHOP-441.
- Honestly: Jira does not replace a test case repo (TestRail/Zephyr); it tracks work, not execution.

## How it works

```gherkin
Feature: Jira workflow for QA

  Scenario: File defect after checkout failure
    Given tester on staging build 4.3.0-rc1
    When payment returns 402 and UI silent
    Then bug SHOP-992 created with Charles session attached
    And fixVersion 4.3.0 and component Checkout set

  Scenario: Verify fix on Ready for QA
    Given developer moves SHOP-992 to Ready for QA
    When QA reruns steps on same build candidate
    Then status transitioned to Done or Reopen with comment

  Scenario: Release burndown query
    Given JQL filter open P1 bugs for fixVersion 4.3.0
    When count greater than zero
    Then release manager blocks prod deploy until triaged
```

Sample JQL:

```
project = SHOP AND issuetype = Bug AND priority in (Highest, High)
AND status in ("Open", "In Progress", "Reopened")
AND labels = regression-4.3.0
ORDER BY priority DESC, created ASC
```

Integrations: Bitbucket commit `SHOP-992` in message, Confluence spec link, Slack automation. Common custom fields: Environment, Steps to Reproduce, Found in Version.

## Pitfalls

- **Vague title "Payment broken."** Not searchable; specific symptom plus endpoint.
- **Priority inflation.** Everything Blocker; agree on severity vs priority matrix.
- **Duplicate flood.** Search before create; use duplicate link.
- **Missing fixVersion.** Fixed bug outside release notes scope.
- **Comment instead of status.** Stale workflow; transition with evidence.
