# ORM и raw SQL в Python (SQLAlchemy)

Как Python разговаривает с базой: от низкоуровневого DB-API (`sqlite3`) с параметризацией и транзакциями до ORM SQLAlchemy 2.x в современном стиле - declarative-модели с `Mapped`, запросы через `select()`, связи `relationship()` и борьба с проблемой N+1.

Generated via `lesson-forge`.
