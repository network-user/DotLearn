exercises:
  - id: super-purpose-001
    concept: inheritance
    type: theory-quiz
    difficulty: 1
    prompt: "Зачем вызывать `super().__init__(...)` внутри конструктора подкласса?"
    choices:
      - id: a
        text: "Без этого программа не скомпилируется."
      - id: b
        text: "Он выполняет инициализацию родительского класса, чтобы унаследованные атрибуты были созданы."
      - id: c
        text: "Он удаляет атрибуты родительского класса, чтобы начать с чистого листа."
      - id: d
        text: "Это чисто документационный приём; во время выполнения он ничего не делает."
    correct: [b]
    explanation: "Когда подкласс переопределяет `__init__`, родительский `__init__` больше не вызывается автоматически. `super().__init__(...)` передаёт аргументы родителю, чтобы атрибуты, задаваемые там (например `self.name`), действительно были созданы."

  - id: loud-dog-output-001
    concept: inheritance
    type: predict-output
    difficulty: 3
    prompt: "Что выведет эта программа?"
    snippet: |
      class Animal:
          def __init__(self, name):
              self.name = name

          def speak(self):
              return f"{self.name} makes a sound"

      class Dog(Animal):
          def speak(self):
              return f"{self.name} barks"

      class LoudDog(Dog):
          def speak(self):
              return super().speak().upper()

      print(LoudDog("Rex").speak())
    expected:
      kind: stdout
      value: "REX BARKS"
    hints:
      - "`LoudDog.speak` вызывает `super().speak()` - это разрешается в `Dog.speak`."
      - "`Dog.speak` возвращает `'Rex barks'`; `.upper()` переводит строку в верхний регистр."

  - id: subclass-blanks-001
    concept: inheritance
    type: fill-in-blanks
    difficulty: 2
    prompt: "Дополните `Cat` так, чтобы он наследовался от `Animal` и переиспользовал родительский конструктор."
    template: |
      class Animal:
          def __init__(self, name):
              self.name = name

      class Cat({{base}}):
          def __init__(self, name, indoor=True):
              {{super_call}}.__init__(name)
              self.indoor = indoor
    blanks:
      base:
        accept: ["Animal"]
      super_call:
        accept: ["super()"]
        accept_regex: "^super\\(\\)$"
    hints:
      - "Укажите родителя в скобках после имени подкласса."
      - "`super()` (без аргументов) возвращает прокси к родительскому классу."
